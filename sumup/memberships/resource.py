# Code generated by `py-sdk-gen`. DO NOT EDIT.
# ruff: noqa: F401, F541
"""
Endpoints to manage user's memberships. Memberships are used to connect the user to merchant accounts and to grant them access to the merchant's resources via roles.
"""

from __future__ import annotations

import datetime
import typing

import httpx
import pydantic
import typing_extensions

from .._exceptions import APIError
from .._service import (
    NOT_GIVEN,
    AsyncResource,
    HeaderTypes,
    NotGivenType,
    Resource,
    serialize_query_params,
    serialize_request_data,
)
from ..types import (
    Attributes,
    Invite,
    Membership,
    MembershipResource,
    MembershipStatus,
    MembershipStatusInput,
    Metadata,
    Problem,
    ResourceType,
    ResourceTypeInput,
)


class ListMembershipsParamsResourceParentTypeInput(typing_extensions.TypedDict, total=False):
    """
    ListMembershipsParamsResourceParentType is a schema definition.
    """



class ListMemberships200Response(pydantic.BaseModel):
    """
    ListMemberships200Response is a schema definition.
    """

    items: list[Membership]

    total_count: int


class MembershipsResource(Resource):
    """API resource for the Memberships endpoints."""

    def __init__(self, client: httpx.Client) -> None:
        super().__init__(client)

    def list(
        self,
        *,
        offset: int | NotGivenType = NOT_GIVEN,
        limit: int | NotGivenType = NOT_GIVEN,
        kind: ResourceTypeInput | NotGivenType = NOT_GIVEN,
        status: MembershipStatusInput | NotGivenType = NOT_GIVEN,
        resource_type: ResourceTypeInput | NotGivenType = NOT_GIVEN,
        resource_attributes_sandbox: bool | NotGivenType = NOT_GIVEN,
        resource_name: str | NotGivenType = NOT_GIVEN,
        resource_parent_id: str | NotGivenType = NOT_GIVEN,
        resource_parent_type: ListMembershipsParamsResourceParentTypeInput
        | NotGivenType = NOT_GIVEN,
        roles: typing.Sequence[str] | NotGivenType = NOT_GIVEN,
        headers: HeaderTypes | None = None,
    ) -> ListMemberships200Response:
        """
        List memberships

        List memberships of the current user.


        Raises:
            APIError: Raised when the API returns one of the documented error responses:
                400: Invalid query parameter combination.
                401: Authentication failed or missing required scope.
                Unexpected response statuses also raise this exception.
        """
        query_data: dict[str, typing.Any] = {}
        if not isinstance(offset, NotGivenType) and offset is not None:
            query_data["offset"] = offset
        if not isinstance(limit, NotGivenType) and limit is not None:
            query_data["limit"] = limit
        if not isinstance(kind, NotGivenType) and kind is not None:
            query_data["kind"] = kind
        if not isinstance(status, NotGivenType) and status is not None:
            query_data["status"] = status
        if not isinstance(resource_type, NotGivenType) and resource_type is not None:
            query_data["resource.type"] = resource_type
        if (
            not isinstance(resource_attributes_sandbox, NotGivenType)
            and resource_attributes_sandbox is not None
        ):
            query_data["resource.attributes.sandbox"] = resource_attributes_sandbox
        if not isinstance(resource_name, NotGivenType) and resource_name is not None:
            query_data["resource.name"] = resource_name
        if not isinstance(resource_parent_id, NotGivenType) and resource_parent_id is not None:
            query_data["resource.parent.id"] = resource_parent_id
        if not isinstance(resource_parent_type, NotGivenType) and resource_parent_type is not None:
            query_data["resource.parent.type"] = resource_parent_type
        if not isinstance(roles, NotGivenType) and roles is not None:
            query_data["roles"] = list(roles)

        resp = self._client.get(
            f"/v0.1/memberships",
            params=serialize_query_params(query_data) if query_data else None,
            headers=headers,
        )
        if resp.status_code == 200:
            return pydantic.TypeAdapter(ListMemberships200Response).validate_python(resp.json())
        elif resp.status_code == 400:
            raise APIError(
                "Invalid query parameter combination.", status=resp.status_code, body=resp.text
            )
        elif resp.status_code == 401:
            raise APIError(
                "Authentication failed or missing required scope.",
                status=resp.status_code,
                body=resp.text,
            )
        else:
            raise APIError(f"Unexpected response", status=resp.status_code, body=resp.text)


class AsyncMembershipsResource(AsyncResource):
    """Async API resource for the Memberships endpoints."""

    def __init__(self, client: httpx.AsyncClient) -> None:
        super().__init__(client)

    async def list(
        self,
        *,
        offset: int | NotGivenType = NOT_GIVEN,
        limit: int | NotGivenType = NOT_GIVEN,
        kind: ResourceTypeInput | NotGivenType = NOT_GIVEN,
        status: MembershipStatusInput | NotGivenType = NOT_GIVEN,
        resource_type: ResourceTypeInput | NotGivenType = NOT_GIVEN,
        resource_attributes_sandbox: bool | NotGivenType = NOT_GIVEN,
        resource_name: str | NotGivenType = NOT_GIVEN,
        resource_parent_id: str | NotGivenType = NOT_GIVEN,
        resource_parent_type: ListMembershipsParamsResourceParentTypeInput
        | NotGivenType = NOT_GIVEN,
        roles: typing.Sequence[str] | NotGivenType = NOT_GIVEN,
        headers: HeaderTypes | None = None,
    ) -> ListMemberships200Response:
        """
        List memberships

        List memberships of the current user.


        Raises:
            APIError: Raised when the API returns one of the documented error responses:
                400: Invalid query parameter combination.
                401: Authentication failed or missing required scope.
                Unexpected response statuses also raise this exception.
        """
        query_data: dict[str, typing.Any] = {}
        if not isinstance(offset, NotGivenType) and offset is not None:
            query_data["offset"] = offset
        if not isinstance(limit, NotGivenType) and limit is not None:
            query_data["limit"] = limit
        if not isinstance(kind, NotGivenType) and kind is not None:
            query_data["kind"] = kind
        if not isinstance(status, NotGivenType) and status is not None:
            query_data["status"] = status
        if not isinstance(resource_type, NotGivenType) and resource_type is not None:
            query_data["resource.type"] = resource_type
        if (
            not isinstance(resource_attributes_sandbox, NotGivenType)
            and resource_attributes_sandbox is not None
        ):
            query_data["resource.attributes.sandbox"] = resource_attributes_sandbox
        if not isinstance(resource_name, NotGivenType) and resource_name is not None:
            query_data["resource.name"] = resource_name
        if not isinstance(resource_parent_id, NotGivenType) and resource_parent_id is not None:
            query_data["resource.parent.id"] = resource_parent_id
        if not isinstance(resource_parent_type, NotGivenType) and resource_parent_type is not None:
            query_data["resource.parent.type"] = resource_parent_type
        if not isinstance(roles, NotGivenType) and roles is not None:
            query_data["roles"] = list(roles)

        resp = await self._client.get(
            f"/v0.1/memberships",
            params=serialize_query_params(query_data) if query_data else None,
            headers=headers,
        )
        if resp.status_code == 200:
            return pydantic.TypeAdapter(ListMemberships200Response).validate_python(resp.json())
        elif resp.status_code == 400:
            raise APIError(
                "Invalid query parameter combination.", status=resp.status_code, body=resp.text
            )
        elif resp.status_code == 401:
            raise APIError(
                "Authentication failed or missing required scope.",
                status=resp.status_code,
                body=resp.text,
            )
        else:
            raise APIError(f"Unexpected response", status=resp.status_code, body=resp.text)
